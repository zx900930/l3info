#ifndef _STACK_H_
#define _STACK_H_

typedef struct maillon{
	char * item;
	struct maillon * next;
	} maillon;

typedef maillon * Stack;

int pushStack(Stack *s, char * elt);
char* popStack(Stack *s);
void emptyStack(Stack *s);
int sizeofStack(Stack s);
void displayStack(Stack s);



#endif
